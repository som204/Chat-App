/**
 * The function `aiGetResult` takes a prompt from the request query, generates an answer using an AI
 * service, and sends the result in the response.
 * @param req - The `req` parameter in the code snippet represents the request object in an Express
 * route handler function. It contains information about the HTTP request that was made, such as the
 * request headers, query parameters, body content, and more. In this specific context, `req.query` is
 * used to access the
 * @param res - The `res` parameter in the code snippet refers to the response object in an Express
 * route handler function. It is used to send a response back to the client making the request. In this
 * case, the `res.send(result)` statement is sending the result generated by the
 * `aiService.generateAnswer(prompt
 */
import * as aiService from '../Services/ai.service.js'


export const aiGetResult= async (req,res)=>{
    try {
        const {prompt}=req.query;
        const result=await aiService.generateAnswer(prompt)
        res.send(result);
    } catch (error) {
        console.log(error);
        res.send(error);
    }
}